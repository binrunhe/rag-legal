1.民法典数据处理成json
2.构建语义向量数据库        embedding模型(qwen 0.6b)和向量数据库(chromadb)
3.main函数先后调用search 和 rag生成回答  不过出现bug,追问时不论什么问题都塞进search,程序漏洞,计划用history数组保留上下文同时使用rewrite_query函数结合history重写用户的query生成新的query
4.加入刑法   合并process与inject  search与rag  目前只需要两个py   但是问题在于main函数因为直接调用了rag文件,实际过程中模型每一次对话都会生成,后续解决一次加载多次调用
5.进一步优化文件处理逻辑,法律名与后面可能不是_而是空格
6.加入requirements和README
7.引入司法解释   这里排除误区,先照法律原文再找司法解释是不可行的,有可能法律原文一开始就定位不到   所以我们多路搜索  都放进同一个collection 靠Metadata严格区分
8. 在处理司法解释的数据处理函数前,优化了法律的处理函数,用is_inside_article来判断当前是否还在法条内,用于处理同个法条下的连续段问题
9.实现司法解释的数据处理函数
10.小逻辑优化,现在先完善单独功能,项目最后再写个一键脚本吧
11.引入rerank(retrieve粗排+rerank细排),目前是简单的基于BGE-Reranker的.解决预料间相似但不相关 把 query 和 每一条候选文档拼在一起，直接判断相关性分数，选出分数最高的前k条,之前的单靠向量搜索精度还是不够,可能很相近但不相关
不过这里要把向量检索用的阈值删掉了,我们要尽可能的多检索到,然后给rerank判断谁分数高选谁
12.优化main函数,加入rerank完成,暂时未完成模型加载一次问题的优化,且终端显示还可以美化,能进一步显示当前已经到了什么流程
13.优化,模型只需加载一次优化(数据库被污染了,等后面写一个一键入库重新入一次)
14.现在我们将process_injest推导重来 用一个registry.json(create_registry)来保存文档名,英文名,类别,保存的json文件名之间的关系...以此来批量调用注入函数
删除English_name,直接用中文名称代替
16.registry结束,开始批量处理与injest